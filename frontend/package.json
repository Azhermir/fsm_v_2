{
  "name": "fsm-frontend",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shell",
    "task-management"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "test": "lerna run test",
    "install:all": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
